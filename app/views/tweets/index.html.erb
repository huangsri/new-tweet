<h1>index</h1>
<% if user_signed_in? %>
  <h2>Welcome, <%= current_user.username if current_user %></h2>
  <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to 'Login', new_user_registration_path %>
  <%= link_to 'Register', new_user_registration_path %>
<% end %>
